replicaCount: 3
revisionHistoryLimit: 3

image:
  repository: ghcr.io/mingleflix/video-service
  pullPolicy: IfNotPresent
  tag: latest

service:
  type: ClusterIP
  port: 80
  targetPort: 3002

containerPort: 3002

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi

imagePullSecrets:
  - name: ghcr-io-secret

jwtSecret:
  name: jwt-secret
  key: JWT_SECRET

env:
  REDIS_HOST: room-management-redis-master